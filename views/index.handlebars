<h1>KRABBY PATTIES!</h1>

{{!-- <img src="../public/assets/img/kp.png" alt="krabby patty"> --}}

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burger/burger-block burgerDevoured=true}}
    {{/unless}}
  {{/each}}
</ul>


<ul>
  {{#each burger}}
    {{#if devoured}}
      {{> burger/burger-block burgerDevoured=false}}
    {{/if}}
  {{/each}}
</ul>


<h2>Add a Krabby Patty!</h2>

  <div class="form-group">
    <label for="burgerAdd">Cat Name:</label>
    <input type="text" id="burgerAdd" name="name">
  </div>

<form class="create-form">
  <div class="form-group">
    <label for="burgerAdd">Krabby Patty:</label>
    <input type="text" id="burgerAdd" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>